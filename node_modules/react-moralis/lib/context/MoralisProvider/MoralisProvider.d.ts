import React from "react";
import { OnAccountChanged } from "../../hooks/useMoralis/_useMoralisAuth";
import { Environment, PluginSpecs } from "../../hooks/useMoralis/_useMoralisInit";
interface MoralisProviderOptions {
    onAccountChanged?: OnAccountChanged;
}
export interface MoralisProviderProps {
    children: React.ReactNode;
    appId: string;
    serverUrl: string;
    jsKey?: string;
    dangerouslyUseOfMasterKey?: string;
    plugins?: PluginSpecs[];
    options?: MoralisProviderOptions;
    environment?: Environment;
}
export declare const MoralisProvider: ({ children, appId, jsKey, dangerouslyUseOfMasterKey, serverUrl, plugins, environment, options: { onAccountChanged }, }: MoralisProviderProps) => JSX.Element;
export {};
